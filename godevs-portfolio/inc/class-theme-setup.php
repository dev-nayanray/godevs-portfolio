<?php
/**
 * Core theme support declarations (add_theme_support, nav menus, etc.).
 *
 * @package GoDevs_Portfolio
 */

namespace GoDevs_Portfolio;

defined( 'ABSPATH' ) || exit;

/**
 * Registers core WordPress theme support flags and other one-time setup.
 *
 * TODO: Phase 1 — add_theme_support() calls (title-tag, post-thumbnails,
 * editor-styles, wp-block-styles, responsive-embeds, html5, etc.) and
 * register_nav_menus() once navigation structure is decided.
 */
class Theme_Setup {

	/**
	 * Hook this class's callbacks into WordPress.
	 */
	public static function init() {
		// TODO: Phase 1 — wire up add_action( 'after_setup_theme', ... ).
	}
}
